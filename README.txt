Peters & Adam - Site V11 logo vectorisé

Base : V10 validée.
Ajouts : logo principal SVG, icône SVG, version blanche SVG, favicon, intégration du SVG dans les pages.
Important : extraire le ZIP avant d'ouvrir index.html.
